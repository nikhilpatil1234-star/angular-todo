import { SetBackGroundColorBlueDirective } from './set-back-ground-color-blue.directive';

describe('SetBackGroundColorBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackGroundColorBlueDirective();
    expect(directive).toBeTruthy();
  });
});
