import { StructuralExampleDirective } from './structural-example.directive';

describe('StructuralExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new StructuralExampleDirective();
    expect(directive).toBeTruthy();
  });
});
